{
  "name": "gabest-wedding-invitation",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vercel dev --listen 3000",
    "build": "echo 'Static build complete'",
    "start": "node -e 'console.log(\"No server to start\")'"
  },
  "dependencies": {
    "airtable": "^0.12.2",
    "dotenv": "^16.3.1",
    "qrcode": "^1.5.3"
  }
}
